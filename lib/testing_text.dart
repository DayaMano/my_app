import 'package:flutter/material.dart';

class TestingText extends StatelessWidget {
  const TestingText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Testing purpose'),
    );
  }
}
